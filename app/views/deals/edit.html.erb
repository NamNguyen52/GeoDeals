<h1>Edit</h1>

<%= form_for @deal, url: deal_edit_path do |d| %>

	<%= d.label :name %>
	<%= d.text_field :name %>

	<%= d.label :description %>
	<%= d.text_area :description, size: "50x10" %>

	<%= d.label :fine %>
	<%= d.text_area :fine, size: "50x10" %>

	<%= d.label :start_date %>
	<%= d.date_select :start_date %>

	<%= d.label :end_date %>
	<%= d.date_select :end_date %>

	<%= d.submit "submit update" %>

<% end %>


<%= link_to "Go Back", index_path %>
